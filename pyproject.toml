[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "ai-config-kit"
version = "0.6.0"
description = "Manage ~/.claude and cross-vendor AI agent rules: 7-vendor adapters, decision packs, AGENTS.md composition."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Simtabi LLC", email = "opensource@simtabi.com" }]
keywords = ["claude", "claude-code", "dotfiles", "symlink", "config-manager"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: MacOS",
  "Operating System :: POSIX :: Linux",
  "Operating System :: Microsoft :: Windows",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development",
  "Topic :: System :: Installation/Setup",
  "Topic :: Utilities",
]
requires-python = ">=3.10"
dependencies = []  # stdlib only: argparse, pathlib, subprocess, json, fnmatch

[project.optional-dependencies]
dev = [
  "pytest>=8.3",
  "pytest-mock>=3.14",
  "ruff>=0.9",
  "mypy>=1.13",
]
# `pip install 'ai-config-kit[s3]'` enables the future S3 sync target
# (SPEC Phase E). The skeleton is wired today; full implementation is
# pending the auth-design ADR.
s3 = ["boto3>=1.34"]

[project.scripts]
ai-config-kit = "ai_config_kit.cli:main"

[project.urls]
Homepage      = "https://opensource.simtabi.com/products/ai-config-kit"
Documentation = "https://opensource.simtabi.com/documentation/ai-config-kit"
Repository    = "https://github.com/simtabi/ai-config-kit"
Issues        = "https://github.com/simtabi/ai-config-kit/issues"
Changelog     = "https://github.com/simtabi/ai-config-kit/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/ai_config_kit"]

[tool.hatch.build.targets.wheel.force-include]
"src/ai_config_kit/py.typed" = "ai_config_kit/py.typed"

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers --strict-config"
filterwarnings = [
  "error",
  "ignore::DeprecationWarning",
  "ignore::PendingDeprecationWarning",
]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src/ai_config_kit"]
